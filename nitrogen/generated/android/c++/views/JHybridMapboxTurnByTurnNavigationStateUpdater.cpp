///
/// JHybridMapboxTurnByTurnNavigationStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "JHybridMapboxTurnByTurnNavigationStateUpdater.hpp"
#include "views/HybridMapboxTurnByTurnNavigationComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JNISharedPtr.hpp>

namespace margelo::nitro::mapboxturnbyturnnavigation::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridMapboxTurnByTurnNavigationState>;

void JHybridMapboxTurnByTurnNavigationStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridMapboxTurnByTurnNavigationSpec::javaobject> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  JHybridMapboxTurnByTurnNavigationSpec* view = javaView->cthis();
  
  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};

  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::dynamic_pointer_cast<const ConcreteStateData>(state);
  const HybridMapboxTurnByTurnNavigationState& data = concreteState->getData();
  const std::optional<HybridMapboxTurnByTurnNavigationProps>& maybeProps = data.getProps();
  if (!maybeProps.has_value()) {
    // Props aren't set yet!
    throw std::runtime_error("HybridMapboxTurnByTurnNavigationState's data doesn't contain any props!");
  }
  const HybridMapboxTurnByTurnNavigationProps& props = maybeProps.value();
  if (props.mute.isDirty) {
    view->setMute(props.mute.value);
    // TODO: Set isDirty = false
  }
  if (props.distanceUnit.isDirty) {
    view->setDistanceUnit(props.distanceUnit.value);
    // TODO: Set isDirty = false
  }
  if (props.origin.isDirty) {
    view->setOrigin(props.origin.value);
    // TODO: Set isDirty = false
  }
  if (props.destinationTitle.isDirty) {
    view->setDestinationTitle(props.destinationTitle.value);
    // TODO: Set isDirty = false
  }
  if (props.destination.isDirty) {
    view->setDestination(props.destination.value);
    // TODO: Set isDirty = false
  }
  if (props.language.isDirty) {
    view->setLanguage(props.language.value);
    // TODO: Set isDirty = false
  }
  if (props.travelMode.isDirty) {
    view->setTravelMode(props.travelMode.value);
    // TODO: Set isDirty = false
  }
  if (props.shouldSimulateRoute.isDirty) {
    view->setShouldSimulateRoute(props.shouldSimulateRoute.value);
    // TODO: Set isDirty = false
  }
  if (props.waypoints.isDirty) {
    view->setWaypoints(props.waypoints.value);
    // TODO: Set isDirty = false
  }
  if (props.showsEndOfRouteFeedback.isDirty) {
    view->setShowsEndOfRouteFeedback(props.showsEndOfRouteFeedback.value);
    // TODO: Set isDirty = false
  }
  if (props.showCancelButton.isDirty) {
    view->setShowCancelButton(props.showCancelButton.value);
    // TODO: Set isDirty = false
  }
  if (props.hideStatusView.isDirty) {
    view->setHideStatusView(props.hideStatusView.value);
    // TODO: Set isDirty = false
  }

  // Update hybridRef if it changed
  if (props.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props.hybridRef.value;
    if (maybeFunc.has_value()) {
      auto shared = JNISharedPtr::make_shared_from_jni<JHybridMapboxTurnByTurnNavigationSpec>(jni::make_global(javaView));
      maybeFunc.value()(shared);
    }
    // TODO: Set isDirty = false
  }
}

} // namespace margelo::nitro::mapboxturnbyturnnavigation::views
